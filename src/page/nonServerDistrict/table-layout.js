var tableLayout = function(table){
       this.scrollY = false;
       this.undateScrollY = function(){
            const bodyWarpper = this.table.bodyWarpper;
            body = bodyWarpper.querySelector("table");
            //  tihs.scrollY =
       }
 }

export default tableLayout;
